(in-package "MRS")

;;; extra globals defined here to keep 
;;; generation code from impacting WK

(defparameter *rel-name-path-only* nil
  "if set, the indexing code only looks for the rel on the
   rel-name-path")


