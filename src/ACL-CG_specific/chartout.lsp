(in-package :cl-user)

;;; Functions below here are to replace functions
;;; which aren't defined yet

(defun close-existing-chart-windows nil
  nil)

(defun display-edge-in-chart (edge-record)
   (declare (ignore edge-record))
   nil)
