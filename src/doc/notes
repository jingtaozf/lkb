Creating screen dumps

1) xwd -out lkbtop-unix
2) xpr -portrait -psfig -width 4 lkbtop-unix > lkbtop-unix.ps

   xpr -portrait -psfig -width 6 loadscript2 > loadscript.ps
   xpr -portrait -psfig -width 8 loadmess2 > loadmess.ps
   xpr -portrait -psfig -width 8 typehier > typehier.ps
   xpr -portrait -psfig -width 5 nelist > ne-list.ps
   xpr -portrait -psfig -width 6 grule > grule.ps
   xpr -portrait -psfig -width 6 rulefs > rulefs.ps
   xpr -portrait -psfig -width 4 kimsleeps > kimsleeps.ps
   xpr -portrait -psfig -width 4 ptree > ptree.ps
   xpr -portrait -psfig -width 6 bigfs > bigfs.ps

\usepackage{times} works now

for PDF hyperlinks I am using

\usepackage[dvips]{hyperref}

this works on eon and eoan, but not on turing apparently

To construct pdf

LaTeX as normal
dvips -Ptype1 -file newdoc.dvi > newdoc.ps
distill newdoc.ps (on turing, or eoan, accidentally ...)
web files are on ~aac/WWW
so 
cp newdoc.pdf ~aac/WWW/doc5-2.pdf


To print two-up:

pstops '2:0L@.7(9in,-.42in)+1L@.7(9in,4.785in)' newdoc.ps >newdoc2.ps

Remaining (known) problems

Generally missing stuff is indicated by FIX (either as LaTeX comment,
or visible in the text).

